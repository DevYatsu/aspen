@Rng_array rng {
    >> [...rng]
}

let val     input("how are u? ")?
// Same as:
// let val  input("how are u? ")
// if val.is_error() {
//    print(val)
// }


@coerce_string value {
    if type(value) != "String" {
        return Err("Value must be of type 'String'!")
    }

    >> value
}

@trim str {
    let value   coerce_string(str)?

    >> value.trim()
}

print(trim(val)?)
