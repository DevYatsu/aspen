
// these are tests, not sth that should be working
while getx("str", 2) + 2 == int("233").power(2) && expr != nil {
    print("nothing")
}

for i -> [1,2,3] {print(str(i))}

if false {
    // never happens
} otherwise true {
    // happens
} else {
    // never happens
}
