while getx("str", 2) + 2 == int("233").power(2) {
    print("nothing")
}

for i -> [1,2,3] {print(str(i))}

if false {
    // never happens
} otherwise true {
    // happens
} else {
    // never happens
}
